export interface LostArkMarketEntry {
  id: string,
  name: string,
  avgPrice: number;
  lowPrice: number;
  image: string;
  recentPrice: number;
  cheapestRemaining: number;
  updatedAt: string;
}
